# Launch Page Merge Ready

入口文件：`index.html`

资源目录：`assets/`

当前角色：
- `The Weeknd` -> `assets/the-weeknd.png`
- `BeagleDuoDong` -> `assets/beagleduodong.png`
- `Judy` -> `assets/judy.png`

交互说明：
- 底部文字区可横向拖动切换角色。
- 上方图片区也可横向滑动切换角色。
- 已删除 `Nick Wilde`、`Loopy`、`CHILLKAWA`。

注意：
- 页面沿用原模板的 Tailwind CDN 和 Google Fonts 引用；如果合入正式工程，研发可按项目构建规范替换为本地依赖或设计系统样式。
